# i18n Layout

- `index.js` provides locale resolution and key lookup helper.
- `locales/en.json` and `locales/ru.json` store dictionary keys.

Use stable keys across pages and fallback to `en` for missing entries.
