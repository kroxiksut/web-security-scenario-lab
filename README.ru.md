# Web Security Scenario Lab

Статический QA- и regression-стенд для browser-extension детекторов, анализа защитного рендеринга и демонстраций security research в контролируемых средах.

> [!CAUTION]
> Данный проект носит строго образовательный, исследовательский и демонстрационный характер. Он предназначен для показа возможных векторов атак, подозрительных веб-паттернов, проверки защитных инструментов и тестирования решений в области информационной безопасности.
> 
> Проект не предназначен для практического злоупотребления, фишинга, мошенничества или иной вредоносной деятельности. Внутри сценариев могут встречаться случайно сгенерированные ссылки, вводящие в заблуждение подписи, визуально скрытые элементы и другие имитации потенциально опасных техник.
> 
> Пользователь и оператор стенда несут полную ответственность за любые действия, выполняемые при использовании проекта. Авторы и сопровождающие проекта не несут ответственности за возможный вред, убытки или иные последствия, возникшие, в том числе, в результате перехода по случайно сгенерированной ссылке, взаимодействия с демонстрационным контентом или использования материалов вне контролируемого тестового или исследовательского контура.
> 
> Любой запуск или деплой данного проекта должен рассматриваться как использование в контролируемой среде ИБ-тестирования.

## Позиционирование

`Web Security Scenario Lab` — это отдельный local-first проект для генерации воспроизводимых client-side веб-сценариев, предназначенных для валидации browser detectors и смежных browser-security инструментов.

Основные фокусы:

- browser-extension QA
- regression testing
- security research demos
- red team validation в контролируемых средах
- blue team validation в контролируемых средах
- purple team validation в контролируемых средах

В отличие от платформ для симуляции фишинговых кампаний или end-user awareness демонстраций, данный стенд сосредоточен на воспроизводимых client-side паттернах веб-страниц для проверки browser detectors на скрытые или обманные техники рендеринга.

## What This Is Not

Этот проект не является:

- платформой для фишинговых кампаний
- LMS-системой или учебным порталом для пользовательской awareness-подготовки
- набором end-user awareness демо для нетехнической аудитории
- сервисом URL reputation
- crawler-based сканером интернета
- системой live-сбора или массового веб-скрейпинга
- источником доменной репутации или внешней threat intelligence лентой
- фермой браузерной автоматизации для массового зондирования интернета
- заменой для реального threat intel, SOC telemetry или production blocklists

## Быстрый старт

Требуется Node.js `^20.19` / `^22.12` / `>=24` (Vite 8). Эти четыре команды одинаковы на Windows
(PowerShell) и Linux (bash):

```bash
npm run setup                # проверки окружения + обе установки (один раз, на свежем клоне)
npm start                    # проверки, доустановка при необходимости и dev-сервер на :5173
npm run build                # статика в dist/, готова для любого файлового сервера
npm run verify               # typecheck + lint + test + build + smoke
```

Раздавать нужно `dist/`, а не корень репозитория: исходные страницы ссылаются на `src/main.ts`,
который умеет разрешить только сборщик. Для раздачи достаточно `npm run preview` или
`node serve.mjs dist`.

**Пошаговые руководства отдельно под каждую систему** — какой терминал открыть, куда перейти, что
запустить, как раздать сборку, плюс параметры глубоких ссылок (`?seed=`, `?focus=1`, `?lang=`,
`?theme=`): **[docs/ru/getting-started.md](./docs/ru/getting-started.md)**
(английская версия: [docs/en/getting-started.md](./docs/en/getting-started.md)).

## Модель покрытия модулей

Модули соответствуют 1:1 модулям детектора и делятся по тому, на кого нацелена манипуляция.

Нацелены на человека-читателя:

- `visual-manipulation` — контент присутствует в DOM, но убран из поля зрения (22 сценария)
- `link-domain-security` — ссылки, у которых видимая и фактическая цель расходятся (2 сценария)

Нацелены на AI-агента, читающего страницу:

- `trigger-phrases` — инструкции в стиле prompt-injection в тексте и атрибутах (3 сценария)
- `prompt-splitting` — одна инструкция, раздробленная по DOM-узлам или атрибутам (5 сценариев)
- `api-interception` — API-подобные маркеры в атрибутах и заявленный MIME против сигнатуры первых байт (4 сценария)

Все пять модулей содержат настоящие сценарии; заглушек не осталось. Каждый модуль несёт и позитивные
сценарии, и минимум один benign-контроль ложных срабатываний — всего 36 сценариев, 29 позитивных и 7
benign. Что покрывает каждый
модуль — см. [docs/ru/README.md](./docs/ru/README.md); таблица сценариев и оставшиеся пробелы —
в документе конкретного модуля.

## Примечание по вкладу

Pull request'ы приветствуются: новые манифесты сценариев, дополнительные оси покрытия для
существующего модуля, evaluation metadata, расширение матрицы фреймворков и версий. В каждом документе
модуля есть раздел **Планируемое покрытие** — конкретные семейства и benign-контроли, которые ещё не
построены; с него проще всего начать.

## Базовые требования

- Только статический проект: `HTML`, `CSS`, `JavaScript`, `JSON`, ассеты.
- Без backend, базы данных, SSR и серверной бизнес-логики.
- Локальная разработка должна одинаково работать на Windows и Linux.
- Стенд должен открываться в современных браузерах: Chrome, Edge, Firefox, Safari и других Chromium-браузерах.
- С первого этапа нужны `RU/EN`.
- Нужна главная страница с переходами к страницам модулей.
- Нужны не только фиксированные образцы, но и динамическая генерация вариантов.
- Весь runtime должен выполняться в браузере, чтобы не нагружать сервер.
- При деплое должна быть минимальная поверхность атаки: сервер только раздаёт файлы.
- Сценарии должны рассматриваться как benchmark-lite активы для проверки детекторов, а не просто как набор демо-страниц.

## Рекомендуемый стек

- `HTML5`
- `CSS3`
- `Vanilla JavaScript` на `ES modules`; `TypeScript` для движка сценариев и общей
  инфраструктуры там, где он повышает надёжность и поддерживаемость
- `JSON`-манифесты сценариев и evaluation metadata
- `Vite` для локальной разработки и сборки статики
- `Playwright` для локальных и CI-проверок

`Vite` и `Playwright` входят только в контур разработки и тестирования. В продакшн-деплой они не обязательны.

Страницы сценариев могут встраивать популярные веб-фреймворки/библиотеки (React, Vue, Svelte,
Angular, Solid, Preact, Lit/Web Components, jQuery, htmx, Alpine, Tailwind, …). **Этот набор не
фиксирован и со временем может расширяться**, чтобы покрытие детектора соответствовало реальному
вебу. Все такие библиотеки вендорятся/пинятся локально — без runtime-зависимости от CDN.

## Общая структура

- `index.html`: главная страница с каталогом модулей и сценариев.
- `pages/visual-manipulation/`: сценарии по скрытому контенту и CSS-обфускации.
- `pages/link-domain-security/`: сценарии по ссылкам, доменам и гомографам.
- `pages/trigger-phrases/`: сценарии по инструкциям в стиле prompt-injection.
- `pages/prompt-splitting/`: сценарии по инструкциям, раздробленным по DOM.
- `pages/api-interception/`: сценарии по API-подобным атрибутам и несовпадению MIME/сигнатуры.
- `pages/scenarios/`: каталог всех сценариев с группировкой по модулям, строится из манифестов.
- `pages/frameworks/`: матрица покрытия по фреймворкам и версиям.
- `frameworks/<lib>/<vN>/`: тот же сценарий, воспроизведённый на запиненной версии фреймворка.
- `pages/shared/`: общие layout-компоненты и demo-helper'ы.
- `assets/`: шрифты, локальные JS-библиотеки, изображения, образцы фонов.
- `data/scenarios/`: JSON-манифесты и наборы конфигураций для вариативности.
- `data/evaluation/`: expected outcomes, severity, tags и rationale metadata.
- `src/`: движок сценариев, i18n, seeded random, evaluation layer, bootstrap и benchmark-lite UI.

## Функциональные принципы

- Каждая страница сценария может работать в fixed и randomized режимах.
- Случайность должна быть воспроизводимой через `seed`.
- Query parameters должны позволять делать deep link, например:
  - `?lang=ru`
  - `?seed=1042`
  - `?module=visual-manipulation`
  - `?scenario=hidden-text-mixed`
- Динамические изменения содержимого должны происходить только в браузере.
- Подозрительный контент должен генерироваться только из внутренних шаблонов, а не из пользовательского ввода.
- Добавление модуля, сценария или версии фреймворка не должно требовать изменения информационной архитектуры.
- Сценарии должны отдавать evaluation outputs, а не только визуальную вариативность.

## Evaluation Outputs

Каждый зрелый сценарий должен уметь отдавать исследователю такие артефакты, как:

- expected signal
- detector should fire / should not fire
- severity
- tags
- объяснение, почему страница подозрительна или намеренно benign
- coverage dimensions, встроенные в данный сценарий

Долгосрочная цель состоит в том, чтобы превратить стенд в benchmark-lite, а не в разрозненный набор страниц. Поэтому `why flagged` panel и coverage matrix стоит рассматривать как части целевой архитектуры уже сейчас.

## Scope Of The First Release

- landing page и навигация по модулям
- static и dynamic сценарии для всех пяти модулей
- каталог сценариев и матрица покрытия фреймворков, строящиеся из манифестов
- переключение языка `RU/EN`
- scenario reset и reroll
- панель с metadata сценария
- базовые evaluation outputs для реализованных сценариев
- базовая матрица совместимости браузеров
- безопасная static-only модель деплоя

## Карта документов

Основная документация лежит в [`docs/`](./docs/README.ru.md), разделена по языкам и организована по
темам — начинайте с [`docs/ru/index.md`](./docs/ru/index.md) или
[`docs/en/index.md`](./docs/en/index.md).

В `docs/` (у каждого документа есть английское зеркало в `docs/en/`):

- [`getting-started.md`](./docs/ru/getting-started.md): запуск стенда на Windows и Linux
- [`README.md`](./docs/ru/README.md): обзор модулей детекции
- `visual-manipulation.md`, `link-domain-security.md`, `trigger-phrases.md`, `prompt-splitting.md`,
  `api-interception.md`: по одному документу на модуль детекции
- [`adding-frameworks.md`](./docs/ru/adding-frameworks.md): добавление фреймворка или запиненной версии

Пока в корне репозитория (переезжают в `docs/` постепенно):

- `README.md` — англоязычный обзор
- `ARCHITECTURE.md` / `ARCHITECTURE.ru.md`
- `BROWSER-COMPATIBILITY.md`
- `SCENARIO-DYNAMICS.md` / `SCENARIO-DYNAMICS.ru.md`
- `EVALUATION-OUTPUTS.md` / `EVALUATION-OUTPUTS.ru.md`
- `I18N.md`
- `SECURITY-DEPLOYMENT.md`
- `CONTRIBUTING.md`
- `DCO.md`
- `ROADMAP.md` / `ROADMAP.ru.md`


