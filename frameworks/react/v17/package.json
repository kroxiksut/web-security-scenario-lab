{
  "name": "framework-react-v17",
  "private": true,
  "description": "Isolated pinned React 17 for the scenario lab. Nested node_modules so react-dom binds to its matching react (see AI_CONTEXT.md). Not part of the root install; run `npm run frameworks:install`.",
  "dependencies": {
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}
