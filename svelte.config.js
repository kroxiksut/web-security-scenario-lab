// Minimal Svelte config so @sveltejs/vite-plugin-svelte doesn't warn about a missing one.
// Scenario component scripts are plain JS (no TS/preprocessing), so no `preprocess` step is needed.
export default {};
